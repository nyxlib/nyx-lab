{
    "name": "nyx-lab",
    "version": "1.0.0",
    "description": "Nyx Lab",
    "main": "main.js",
    "scripts": {
        "electron-start": "rm -fr ./dist/ && cp -R ../dist/ . && electron .",
        "electron-make-linux": "rm -fr ./dist/ && cp -R ../dist/ . && electron-forge make --platform=linux --arch=x64",
        "electron-make-windows": "rm -fr ./dist/ && cp -R ../dist/ . && electron-forge make --platform=win32 --arch=x64"
    },
    "author": "Jérôme Odier",
    "license": "GPL-3.0-or-later",
    "devDependencies": {
        "@electron-forge/cli": "^7.11.1",
        "@electron-forge/maker-zip": "^7.11.1",
        "@electron-forge/maker-squirrel": "^7.11.1",
        "@reforged/maker-appimage": "^5.2.0",
        "electron": "^41.0.2"
    }
}
